package com.fsoft.kbase.entity.enums;

public enum DocumentType {
    DOCUMENT,   // PDF, DOCX, XLSX, PPTX, TXT, MD
    IMAGE,      // JPG, PNG, GIF, SVG, BMP, WebP
    VIDEO       // MP4, MOV, AVI, WebM
}
